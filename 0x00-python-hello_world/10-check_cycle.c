#include "lists.h"
/**
 * check_cycle - examines for a cycle in a linked list
 * @list: examined linked list
 * Return: 0 if no cycle, else 1
 */
int check_cycle(listint_t *list)
{
	listint_t *snail =list;
	listint_t *hare =list;

	if(!list)
		return(0);

	while(slow&&fast&&fast->next)
	{
		slow
}
