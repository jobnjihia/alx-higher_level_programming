__lists all records of the table second_table of the database hbtn_0c_0 in your MySQL server.
__ display both the score and the name (in this order)
__ordered by score (top first)
__database name will be passed as an argument of the mysql 

SELECT score, name
FROM second_table
ORDER BY score DESC;
